# Task

Create the file `/app/main.typ` containing a single display equation that renders as:

> α with a subscript c, followed by `(N)` as normal function arguments at the baseline, then `= 1/N`.

In plain text: `alpha_c(N) = 1/N`, where `(N)` is *not* part of the subscript.

Compile the document to `/app/main.pdf`.
