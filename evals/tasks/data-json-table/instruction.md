# Task

The file `/app/data.json` contains a JSON array of product records with `name`, `price`, and `stock` fields.

Create `/app/main.typ` that loads this file and typesets its contents as a table with a header row (`Name`, `Price`, `Stock`) and one row per record.

Compile the document to `/app/main.pdf`.
