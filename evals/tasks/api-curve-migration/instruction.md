# Task

Create the file `/app/main.typ` that draws an open polyline through the points (0pt, 0pt), (60pt, 20pt), (120pt, 0pt) — a simple zigzag line — using Typst's built-in drawing elements.

Compile the document to `/app/main.pdf`.
