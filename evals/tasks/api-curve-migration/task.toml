schema_version = "1.4"

[task]
name = "claude-skill-typst/api-curve-migration"
version = "1.0.0"
description = "Draw a polyline with the 0.15 curve element (path was removed)"
authors = [{ name = "lucifer1004" }]
keywords = ["typst", "0.15", "breaking-change"]

[metadata]
category = "typst-skill"
difficulty = "easy"
skill_relevance = "migration.md lists path -> curve removal"

[verifier]
timeout_sec = 300.0

[agent]
timeout_sec = 900.0
