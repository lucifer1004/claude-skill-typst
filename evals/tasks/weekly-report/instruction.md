# Task

Create `/app/main.typ` — a one-page weekly status report containing:

1. The title "Weekly Status Report" (as a level-1 heading).
2. A level-2 heading "Progress" followed by a table with a header row and exactly 3 data rows (task name + status).
3. A level-2 heading "Plans" followed by a short paragraph.
4. Exactly one numbered display equation.

Compile the document to `/app/main.pdf`.
