schema_version = "1.4"

[task]
name = "claude-skill-typst/math-fraction-trap"
version = "1.0.0"
description = "Typeset cal(Z)(tilde(S)) / cal(Z)(0) as a single fraction"
authors = [{ name = "lucifer1004" }]
keywords = ["typst", "math", "silent-render-trap"]

[metadata]
category = "typst-skill"
difficulty = "easy"
skill_relevance = "debug.md documents the slash-binds-one-atom trap"

[verifier]
timeout_sec = 300.0

[agent]
timeout_sec = 900.0
