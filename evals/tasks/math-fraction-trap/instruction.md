# Task

Create the file `/app/main.typ` containing a single display equation showing the ratio

> 𝒵(S̃) over 𝒵(0)

i.e. the calligraphic Z of S-tilde divided by the calligraphic Z of 0, rendered as **one fraction** with the whole `𝒵(S̃)` as numerator and the whole `𝒵(0)` as denominator.

Compile the document to `/app/main.pdf`.
