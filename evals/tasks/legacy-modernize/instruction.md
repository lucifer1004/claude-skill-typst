# Task

The file `/app/draft.typ` was written for Typst 0.12 and no longer compiles
under the current Typst version.

Upgrade it:

1. Port every removed or changed API to its modern equivalent.
2. Preserve the intended rendered output as closely as possible.
3. Save the result as `/app/main.typ` and compile it to `/app/main.pdf`.
